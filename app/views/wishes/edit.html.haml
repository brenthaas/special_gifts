%h1 Editing wish

= render partial: 'form',
  locals: { wish: @wish, submit_url: wish_path(@wish) }

= link_to 'Show', @wish
\|
= link_to "Back To #{@wisher_name} Wishes", user_wishes_path(@wish.wisher)
